<div>
  <center><h1><%= t ".tags" %> (<%= @tags.length %>)</h1></center>
  <section class="custom-padding" id="how-it-work">
    <div class="container">
      <div class="col-md-12">
        <%= render partial: "tags/tag", collection: @tags, as: :tag %>
      </div>
    </div>
    <div class="text-center clearfix">
      <%= paginate @tags %>
    </div>
  </section>
</div>
